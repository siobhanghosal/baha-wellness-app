name: parent_app
description: Parent app shell for the BAHA mobile suite.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  baha_design_system:
    path: ../../packages/baha_design_system
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
