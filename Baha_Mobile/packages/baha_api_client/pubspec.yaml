name: baha_api_client
description: Typed BAHA mobile API client for Flutter apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  baha_shared_models:
    path: ../baha_shared_models
  http: ^1.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
