name: baha_auth_session
description: Shared BAHA session restore and onboarding routing logic.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: '>=1.17.0'

dependencies:
  baha_api_client:
    path: ../baha_api_client
  baha_shared_models:
    path: ../baha_shared_models
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  http: ^1.5.0
flutter:
